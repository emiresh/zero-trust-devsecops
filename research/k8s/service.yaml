apiVersion: v1
kind: Service
metadata:
  name: ai-collector
  namespace: ai-security
  labels:
    app: ai-security-collector
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ai-security-collector
