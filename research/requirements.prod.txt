# Production Dependencies for AI Security Collector
# MINIMAL - Only packages actually imported in code
# No ML, no Jupyter, no unused HTTP clients

# Web Framework (used in falco_collector.py)
fastapi==0.115.6
uvicorn[standard]==0.32.0
pydantic==2.9.0

# Azure OpenAI (used in incident_reporter.py)
openai==1.51.0

# Everything else is Python stdlib:
# - json, os, logging, asyncio, datetime, pathlib, typing, collections

